# ai-coding-ok: v3.1.0
name: CI

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main, develop]

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

env:
  # ai-coding-ok 使用 Shell + Python，无需特定语言版本
  LANGUAGE_VERSION: "3.12"

jobs:
  lint:
    name: "🔍 Lint & Format"
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      # ai-coding-ok 非代码项目，CI 仅做 install/verify 完整性检查
      - name: Set up environment
        run: echo "TODO: set up language and dependencies"

      - name: Check formatting
        run: echo "TODO: run formatter check"

      - name: Check linting
        run: echo "TODO: run linter"

  type-check:
    name: "🔬 Type Check"
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up environment
        run: echo "TODO: set up language and dependencies"

      - name: Run type checker
        run: echo "TODO: run type checker"

  test:
    name: "✅ Test"
    runs-on: ubuntu-latest
    needs: [lint, type-check]
    env:
      # ai-coding-ok 无数据库，无需配置测试环境变量
      DATABASE_URL: "sqlite:///test.db"
      DEBUG: "true"
    steps:
      - uses: actions/checkout@v4

      - name: Set up environment
        run: echo "TODO: set up language and dependencies"

      - name: Run tests with coverage
        run: echo "TODO: run tests"

      - name: Upload coverage report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: coverage-report
          path: coverage.xml

  build:
    name: "🏗️ Build Check"
    runs-on: ubuntu-latest
    needs: [test]
    steps:
      - uses: actions/checkout@v4

      - name: Set up environment
        run: echo "TODO: set up language and dependencies"

      - name: Verify app can start
        run: echo "TODO: verify build"

